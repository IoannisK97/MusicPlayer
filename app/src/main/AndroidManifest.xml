<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jandroid.musicplayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicPlayer">
        <activity android:name=".BuyNewActivity"></activity>
        <activity
            android:name=".Search"
            android:label="Search" />
        <activity
            android:name=".DGuetta"
            android:label="David Guetta" />
        <activity
            android:name=".JCole"
            android:label="J. Cole" />
        <activity
            android:name=".ListOfArtists"
            android:label="List of artists" />
        <activity
            android:name=".NowPlaying"
            android:label="Now playing" />
        <activity
            android:name=".MainActivity"
            android:label="Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />
    </application>

</manifest>